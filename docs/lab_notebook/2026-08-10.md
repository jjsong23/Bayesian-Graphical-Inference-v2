# Lab notebook — 2026-08-10

## Independent binary model adopted for node selection

Node selection was revised to use the same hypothesis structure as edge
characterization. For each candidate protein (i), the mutually exclusive
hypotheses are:

- (H_{1,i}): the protein is present in the signaling system; and
- (H_{0,i}): the protein is not present in the signaling system.

The default objective prior assigns equal probability to the two hypotheses:

$$
P(H_{1,i})=P(H_{0,i})=0.5.
$$

Each protein is a separate Bernoulli hypothesis. The posterior probabilities
are not normalized across the 9,170 candidates and are not required to sum to
one. Consequently, evidence supporting one protein does not reduce the
probability of another protein merely by reallocating a fixed amount of
probability mass.

Protein-abundance and principal-cell RNA source scores retain their historical
range from 0.5 to 1. Before integration, each is divided by its neutral value
0.5:

$$
BF_{i,s}=f_{i,s}/0.5.
$$

The neutral source score therefore becomes (BF=1), and the strongest possible
score approaches (BF=2). Kinase-activity and phosphoprotein-response tables
already store neutral-relative multipliers with (BF=1) as the neutral value.
Missing or inapplicable evidence is also assigned (BF=1).

For enabled stream (s) with user weight (w_s):

$$
O_i^{\mathrm{post}}
=O_i^{\mathrm{prior}}
\prod_s BF_{i,s}^{w_s},
\qquad
P_i^{\mathrm{post}}
=\frac{O_i^{\mathrm{post}}}{1+O_i^{\mathrm{post}}}.
$$

At the default prior of 0.5, prior odds equal one. A protein receiving only
neutral or missing evidence remains exactly at posterior 0.5. The default
selection rule retains proteins whose posterior is strictly greater than 0.5.
The GUI now exposes both the node prior and the selection cutoff.

The previous sum-to-one operation was appropriate only for a categorical model
in which exactly one candidate was assumed to be the relevant node. That model
does not match a signaling network, where many proteins may simultaneously be
present. It was therefore removed from current node inference. Source-score
normalization to a neutral (BF=1) remains necessary; cross-node posterior
normalization does not.

## Implementation and validation

- Added `binary_bayes_factor_update` to `code/bayes_factors.py`.
- Updated the GUI workflow engine to integrate node evidence in log-odds space.
- Added node-prior and node-selection-cutoff controls to the workbench.
- Updated standalone node-integration scripts to use independent binary
  updates.
- Preserved the default selected set: 871 protein nodes plus 20 curated second
  messengers.
- Added tests confirming that neutral evidence preserves 0.5, supported nodes
  can simultaneously exceed 0.5, and sequential BF multiplication is
  commutative.

Historical result files containing node posteriors normalized to sum to one
remain provenance records of the earlier model. They should be regenerated or
clearly labeled before being used as current node-probability results.

## Collecting-duct node-selection extension

Added six node evidence streams for the Aqp2-focused collecting-duct analysis:
rat proteome and mouse renal-tubule RNA abundance for CCD, OMCD, and IMCD.
Each segment is independently selectable, while registry dependence groups
record that the three segments within each assay share a source experiment.

Rat proteome identifiers were harmonized with Ensembl release 116 orthology.
UniProt accessions were preferred, rat gene symbols were used as a fallback,
high-confidence orthologs were preferred when available, and low-confidence
fallbacks were preserved with explicit audit flags. This mapped 7,107 of 7,429
rat proteins to a named mouse ortholog; 2,678 mapped into the 9,170 signaling
candidates, and 322 remained unmapped.

For every assay/segment, zeros were treated as nondetection and excluded from
the empirical q75 background. Positive values were scored with the existing
complement-of-minimum likelihood and divided by the 0.5 neutral likelihood to
produce BF=1 at neutrality. The six T75 values were 66,565.7131, 70,816.4444,
and 65,193.9136 for the CCD/OMCD/IMCD proteomes, and 22.9, 9.2, and 11.7 for the
corresponding RNA streams.

Enabling all six streams in addition to the existing four selected 3,296
proteins, versus the 871-protein baseline. Adding the 20 curated second
messengers produced 3,316 total nodes. The generic GUI default remains unchanged
at 891 nodes; the collecting-duct streams are available but default off.

Generated artifacts:

- `data/node_selection/collecting_duct/raw/`: immutable source copies and
  Ensembl tables.
- `data/node_selection/collecting_duct/processed/`: factor table, thresholds,
  orthology audit, unmapped rat proteins, RNA duplicate audit, and JSON summary.
- `results/collecting_duct_node_selection/`: full posteriors, selected node
  universe, newly selected proteins, cumulative counts, and configuration.
- `outputs/collecting_duct_integration_20260810/collecting_duct_node_evidence_audit.xlsx`:
  human-readable audit workbook with per-node formula verification.
